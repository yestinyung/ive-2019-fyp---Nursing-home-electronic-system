<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fyp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icons8_hospital_sign_50"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icons8_hospital_sign_50"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainPageByDoctor"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ListPatientDetailByDoctor"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AddPatientDetail"
            android:screenOrientation="portrait" />
        <activity
            android:name=".UpdatePatientDetailByDoctor"
            android:screenOrientation="portrait" />
        <activity android:name=".MainPageByAdmin" />
        <activity android:name=".ListPatientDetailByAdmin" />
        <activity android:name=".MainPageByPatient" />
        <activity android:name=".UpdatePatientDetailByAdmin" />
        <activity android:name=".UserProfileByAll" />
        <activity android:name=".ListDoctorDetailByAdmin" />
        <activity android:name=".UpdateDoctorDetailByAdmin" />
        <activity android:name=".AddDoctorDetail" />

        <service android:name=".NickyService" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <activity android:name=".BedControlerByPatient" />
        <activity android:name=".AddMedicalrecord" />
        <activity android:name=".Notification_ListView" />
        <activity android:name=".Notification_closed" />
        <activity android:name=".ListHeartbeatAndBodyTemperature" />
        <activity android:name=".ListRoomDetailByDoctor"></activity>
    </application>

</manifest>